name: Sync OAS to ReadMe
on:
  push:
    branches:
      - master
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: readmeio/github-readme-sync@v2
        with:
          readme-oas-key: 7vLlme1XYiwUXDkXmVZxaLcATS0olh3d:614b799c951f7642e6c41a41
           
          # OPTIONAL CONFIG, use if necessary
          oas-file-path: './output/swagger.json'
          # api-version: 'v2.0.0'
