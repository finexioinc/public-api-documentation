name: Sync OAS to ReadMe
on:
  push:
    branches:
      - master
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: readmeio/github-readme-sync@v2
        with:
          readme-oas-key: bBcDCtTB0i7QyuIzI3JSlAzJgazDoo48:614b7039c02a4c51ae7252ee
           
          # OPTIONAL CONFIG, use if necessary
          oas-file-path: './output/swagger.json'
          # api-version: 'v1.0.0'
