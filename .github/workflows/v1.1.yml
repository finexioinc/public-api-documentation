name: Finexio API V1.1
on:
  push:
    branches:
      - swaggerhub-finexio-api_doc-v1_1
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: readmeio/github-readme-sync@v2
        with:
          readme-oas-key: bBcDCtTB0i7QyuIzI3JSlAzJgazDoo48:61b0aae4efa7d3e53754f5ea
           
          # OPTIONAL CONFIG, use if necessary
          oas-file-path: './output/swagger.json'
          api-version: 'v1.1.0'
